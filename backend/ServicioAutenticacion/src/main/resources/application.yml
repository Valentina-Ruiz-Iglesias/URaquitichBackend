server:
  port: 8081

spring:
  application:
    name: servicio-autenticacion

  datasource:
    #url: jdbc:postgresql://database-raquitich.cbdllehc2ios.us-east-1.rds.amazonaws.com:5432/db_autenticacion?sslmode=require
    #username: postgres
    #password: Raquitich.2026
    #driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5433/db_autenticacion
    username: postgres
    password: 1234
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

  jackson:
    serialization:
      indent-output: true

app:
  jwt:
    secret: cmFxdWl0aWNoX3N1cGVyX3NlY3JldF9rZXlfand0XzIwMjZfcmFxdWl0aWNoX2JhY2tlbmQ=
    expiration: 86400000
  internal:
    api-key: raquitich-internal-secret-2026

management:
  endpoints:
    web:
      exposure:
        include: health,info

  endpoint:
    health:
      show-details: when-authorized

logging:
  level:
    org.springframework.security: INFO
    org.hibernate.SQL: WARN
