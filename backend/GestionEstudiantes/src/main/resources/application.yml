server:
  port: 8082

spring:
  application:
    name: GestionEstudiantes

  datasource:
    url: jdbc:postgresql://localhost:5434/db_estudiantes
    username: postgres
    password: 1234
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true